# No runtime environment variables are required for this static site.
#
# Do not commit real secrets. Static frontend files deployed to Vercel are public
# to anyone who can download the site bundle.
#
# If you later add a backend or build step, keep local values in .env.local.
# .env and .env.* are ignored by .gitignore, while this example file is safe to commit.
